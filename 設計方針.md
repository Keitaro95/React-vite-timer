Timer表示に関係するファイル
- TimerButtonでボタン表示。typeによって展開を変える。
- hooks/useTimer　でtimerの動作を司るカスタムフック
- useTimer.slice で reducer関数を定義
- store/store.tsで reducerを登録


page/page.tsxでレンダリング
// - TimerButtonでボタンコンポーネントをかく。
// - TimerDisplayはタイマー時刻の表示


### frontend
router.tsx：画面表示を司るrouter。パスによって表示を変えるために存在します。

### backend
server.ts：全体的なディレクトリを司ります。Djangoのpjファイルのpathに似ています。

api.ts：細かいapiルーティングにおける動作を設定しています





カウントアップとカウントダウン機能があるタイマー

現在時刻：13:04 ←APIで取得

UI：00:00
時刻部分をクリック：「00:00~60:00の間で時間をセットしてください」ボックス。タイプして入力。
createPortal	0秒になったらトースト通知（createPortal）「カウント完了！」のトースト表示
useEffect	タイマーの更新処理（1秒ごと）

button_スタート（動いている間はスモークがかかる）スタートボタンを押す：「カウントアップ」0からカウント
button_一時停止(一時停止状態だとスモークがかかる)
button_リセット(時刻、stateをリセット)

useReducer	stateを管理しましょう。タイマーの状態（mode, time, isRunning）管理
useRef	setInterval で再生/停止の制御
Redux	stateの管理はreduxのstoreで行う。計測履歴や設定（例：初期カウント時間）の保存。slice.tsを書いてsliceをreduxのstoreに登録

テスト（reducer）タイマーstateの状態遷移テスト（start, pause, resetなど）
テスト（UI）	  トースト表示・ボタンクリック動作の確認
テスト（非同期通信）モックAPI送信



GitHub Actions + GCP を使った CI/CD 自動化ドキュメント
# CI/CD 設計ガイド：TS+React(Vite) + GitHub Actions + GCP + Docker

本ドキュメントでは、以下の三環境を一貫して管理するための設定と構成例をまとめています：

1. **development**（ユニットテスト＋ローカル動作）
2. **stage**（Integration テスト＋GCPステージ環境）
3. **main/production**（Smoke テスト＋GCP本番環境）

---

## 1. GitHub リポジトリ設定

### 1.1 ブランチ保護ルール

| ブランチ            | 制限内容                                                                                                     |
| --------------- | -------------------------------------------------------------------------------------------------------- |
| **development** | 制限なし（自由に push/PR 可能）                                                                                     |
| **stage**       | PR レビュー必須（Require pull request reviews before merging）                                                   |
| **main**        | ・Require status checks to pass<br>・Require pull request reviews<br>・Include administrators<br>直接 push 禁止 |

### 1.2 Environments 設定（Settings > Environments）

リポジトリ環境ごとに承認フローやシークレットを分離します。

| Environment     | 用途                            | シークレット / 保護設定                          |
| --------------- | ----------------------------- | -------------------------------------- |
| **development** | ユニットテスト＋ローカル動作確認              | 特になし（ローカル CI のみ）                       |
| **stage**       | Integration テスト＋GCPステージ環境デプロイ | `WIF_PROVIDER`、`WIF_SA_STAGE`          |
| **production**  | 本番デプロイ                        | `WIF_PROVIDER`、`WIF_SA_PROD` + マニュアル承認 |

---

## 2. GCP 側の設定

### 2.1 Workload Identity Federation

1. GCP コンソール > IAM & 管理 > Workload Identity Federation で GitHub Actions 用プロバイダーを作成
2. リポジトリ or Org を指定し、`sts.googleapis.com` のトークン発行を許可
3. 完全名を `WIF_PROVIDER` として GitHub シークレットに登録

### 2.2 サービスアカウント作成・権限付与

**Stage 用 SA** (`sa-stage@PROJECT_ID.iam.gserviceaccount.com`)

```bash
# SA 作成
gcloud iam service-accounts create sa-stage \
  --display-name="GitHub Actions (Stage)"
# Artifact Registry 書き込み権限
gcloud projects add-iam-policy-binding PROJECT_ID \
  --member="serviceAccount:sa-stage@PROJECT_ID.iam.gserviceaccount.com" \
  --role="roles/artifactregistry.writer"
# Cloud Run 管理権限
gcloud projects add-iam-policy-binding PROJECT_ID \
  --member="serviceAccount:sa-stage@PROJECT_ID.iam.gserviceaccount.com" \
  --role="roles/run.admin"
```

**Production 用 SA** (`sa-prod@PROJECT_ID.iam.gserviceaccount.com`)

* 同様に `roles/artifactregistry.writer`, `roles/run.admin`, `roles/iam.serviceAccountUser` を付与

### 2.3 Cloud Run サービス

* **ステージ環境**: `my-service-stage`
* **本番環境**: `my-service-prod`

---

## 3. Vite + React アプリの Docker 構成

### 3.1 共通の前提

* `npm run dev`: 開発用ホットリロード
* `npm run build`: 本番ビルド (`dist/` 出力)
* `serve -s dist` or `npm run preview`: 静的ホスティング
* `vite.config.ts` で `server.host = '0.0.0.0'`, `port = 3000` を設定※

### 3.2 開発用 Dockerfile (`Dockerfile.dev`)


* コード変更を即時反映するには Docker Compose と合わせてボリュームマウントを利用

### 3.3 本番/ステージ用 Dockerfile (`Dockerfile`)


* Multi-stage で余分な依存を含まない軽量イメージを生成

---

## 4. Docker Compose 例

### 4.1 開発用 (`docker-compose.dev.yml`)


### 4.2 ステージ/本番用 (`docker-compose.yml`)



---

## 5. GitHub Actions CI/CD ワークフロー

`.github/workflows/ci-cd.yml`


**ポイント**

1. `if: github.ref` でブランチごとにジョブ分岐
2. `environment:` で GitHub Environments 保護を適用
3. `needs:` で前段ジョブの成功を担保
4. マルチステージ Dockerfile を使い分け
5. ローカルは Docker run、ステージ／本番は Cloud Run へデプロイ

---

## 6. ローカル起動用 npm スクリプト

`package.json` の `scripts` 例：

```json
{
  "scripts": {
    "deploy:dev":   "gh workflow run ci-cd.yml --ref development",
    "deploy:stage": "gh workflow run ci-cd.yml --ref stage",
    "deploy:prod":  "gh workflow run ci-cd.yml --ref main"
  }
}
```

---

## 7. まとめ表

| 環境          | Dockerfile     | 動作内容                            |
| ----------- | -------------- | ------------------------------- |
| development | Dockerfile.dev | `npm run dev`（ホットリロード）          |
| stage       | Dockerfile     | `npm run build`→ Cloud Run デプロイ |
| main        | Dockerfile     | `npm run build`→ Cloud Run デプロイ |

<br>
※ `vite.config.ts` にて `server.host='0.0.0.0'` を設定しておくこと。

## honoの仕組み
### frontend
router.tsx：画面表示を司るrouter。パスによって表示を変えるために存在します。
### backend
server.ts：appインスタンスを置くところ。全体的なディレクトリを司ります。Djangoのpjファイルのpathに似ています。
handler.ts：サーバーレスサービスでの処理を記述するハンドラ。firebaseならfirebaseにして欲しい処理を書く。




## 開発方針のログ
6/27 firebase上でタイマーが動くかどうか確認するのみにする。
つまり、バックエンドhonoでのルーティングだけ確認する。
firestoreでの会員機能を想定したが、現時点では破棄。
Reactのレンダリング方法
1. ビルドして静的ファイルとして表示
2. viteのアダプタとかhonoのhandlerを使ってReactは変えずSSRで表示
https://chatgpt.com/share/685e2e48-6e24-8007-bff3-8a54ed80aee9
全体的にReact Routerと Hono の連携がわからない。
honoからfirebaseへの開通が、事例がない。
Reactはビルドして使うのがいいのかもしれない。
Buttonのデザインを少し改修
だけど、vite.config.tsを変えたから起動できない。
それから、firebaseを入れる前のUIだけ確認する用のブランチを切っておけばよかった
時間表示のUIが00が表示されない

この方針でいく
以下の設計例では、**Hono** + **hono-sessions** をバックエンドに使い、**React Hook Form** でログインフォームを実装し、**React Router** で認証有無によるルート保護を行います。

---

## ✅ 全体構成

1. **バックエンド（Hono）**

   * `hono-sessions` ミドルウェアで Cookie ベースのセッション管理
   * `POST /api/login`（ログイン）、`POST /api/logout`（ログアウト）、`GET /api/session`（セッション確認） のエンドポイント提供 ([github.com][1])

2. **フロントエンド（React）**

   * **React Hook Form** によるフォーム入力・バリデーション
   * **React Router** を使ったプライベートルートとナビゲーション

---

## 🧰 1. Hono + セッション設定

```ts
import { Hono } from 'hono';
import { sessionMiddleware, CookieStore } from 'hono-sessions';

const app = new Hono();

app.use('*', sessionMiddleware({
  store: new CookieStore(),
  encryptionKey: '32文字以上の乱数',
  cookieOptions: { httpOnly: true, sameSite: 'lax', path: '/' },
  expireAfterSeconds: 60 * 60 * 24, // 24時間有効
}));

app.post('/api/login', async (c) => {
  const { username, password } = await c.req.parseBody();
  if (username === 'user' && password === 'pass') {
    const sess = c.get('session');
    sess.set('user', { username });
    return c.json({ ok: true });
  }
  return c.json({ ok: false }, 401);
});

app.post('/api/logout', (c) => {
  const sess = c.get('session');
  sess.delete('user');
  return c.json({ ok: true });
});

app.get('/api/session', (c) => {
  const sess = c.get('session');
  return c.json({ user: sess.get('user') || null });
});

export default app;
```

* `hono-sessions` によって、サーバー側で Cookie を発行し、セッション情報（ここでは `user`）を保存します 。
* `/api/session` で現在のログイン状態を返します。

---

## 🧩 2. React: ログインフォーム（React Hook Form）

```tsx
import { useForm } from 'react-hook-form';

export function Login() {
  const { register, handleSubmit, formState: {errors} } = useForm();
  const onSubmit = async (data) => {
    const res = await fetch('/api/login', {
      method: 'POST',
      headers: {'Content-Type':'application/json'},
      body: JSON.stringify(data),
    });
    if (res.ok) window.location.href = '/dashboard';
    else alert('Invalid credentials');
  };

  return (
    <form onSubmit={handleSubmit(onSubmit)}>
      <div>
        <input {...register('username', { required: true })} placeholder="Username" />
        {errors.username && <span>必須です</span>}
      </div>
      <div>
        <input type="password" {...register('password', { required: true })} placeholder="Password" />
        {errors.password && <span>必須です</span>}
      </div>
      <button type="submit">ログイン</button>
    </form>
  );
}
```

* React Hook Form を使えば、フォームの状態管理やバリデーションがシンプルに行えます。
* `onSubmit` でバックエンド API に送信します。成功したら認証済みルートへ遷移。

---

## 🔐 3. React Router：認証ルート設定

```tsx
import { BrowserRouter, Routes, Route, Navigate } from 'react-router-dom';
import { useEffect, useState } from 'react';

function PrivateRoute({ children }) {
  const [loading, setLoading] = useState(true);
  const [user, setUser] = useState(null);

  useEffect(() => {
    fetch('/api/session').then(r => r.json()).then(j => {
      setUser(j.user);
      setLoading(false);
    });
  }, []);

  if (loading) return <div>Loading...</div>;
  return user ? children : <Navigate to="/login" />;
}

function App() {
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/login" element={<Login />} />
        <Route path="/" element={<PrivateRoute><Dashboard /></PrivateRoute>} />
      </Routes>
    </BrowserRouter>
  );
}
```

* `/` はログイン済みユーザーのみアクセス可。
* ログイン状態が無ければ `/login` にリダイレクト。

---

## 💡 実装のポイントまとめ

* **Hono** ＋ `hono-sessions` でサーバー側セッション処理を一貫管理 ([gitlab.com][2], [youtube.com][3], [medium.com][4], [reddit.com][5], [github.com][1])
* **React Hook Form** でクライアント側フォームバリデーションと状態管理
* **React Router** でログイン状態を元に保護されたルートを構築

---

✨ この構成であれば、ログインからセッション維持、パス保護まで安全かつ効率的に実装できます。

必要であれば **ログアウトボタン** の実装や、**サーバー検証付きリダイレクト**（`loader` を使った SSR 認証チェック）など、さらに実用的にカスタマイズする方法もお伝えできます。お気軽に！

[1]: https://github.com/jcs224/hono_sessions?utm_source=chatgpt.com "jcs224/hono_sessions: Cookie-based sessions for Hono applications"
[2]: https://gitlab.com/ChristianSirolli/hono-jwt-auth-middleware?utm_source=chatgpt.com "Hono JWT Auth Middleware - Christian Sirolli - GitLab"
[3]: https://www.youtube.com/watch?v=LFYJYRP9VP8&utm_source=chatgpt.com "Managing Form State | React Hook Form Tutorial - YouTube"
[4]: https://medium.com/%40aymane.sarrar/create-a-simple-authentication-with-hono-and-otplib-authenticator-692f4de69d5d?utm_source=chatgpt.com "create a simple authentication with hono and otplib (authenticator)"
[5]: https://www.reddit.com/r/nextjs/comments/1ghkp33/getting_the_user_session_in_a_server_component/?utm_source=chatgpt.com "Getting the user session in a server component using Hono + Next ..."
