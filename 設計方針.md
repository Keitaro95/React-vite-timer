Timer表示に関係するファイル
- TimerButtonでボタン表示。typeによって展開を変える。
- hooks/useTimer　でtimerの動作を司るカスタムフック
- useTimer.slice で reducer関数を定義
- store/store.tsで reducerを登録


page/page.tsxでレンダリング
// - TimerButtonでボタンコンポーネントをかく。
// - TimerDisplayはタイマー時刻の表示





カウントアップとカウントダウン機能があるタイマー

現在時刻：13:04 ←APIで取得

UI：00:00
時刻部分をクリック：「00:00~60:00の間で時間をセットしてください」ボックス。タイプして入力。
createPortal	0秒になったらトースト通知（createPortal）「カウント完了！」のトースト表示
useEffect	タイマーの更新処理（1秒ごと）

button_スタート（動いている間はスモークがかかる）スタートボタンを押す：「カウントアップ」0からカウント
button_一時停止(一時停止状態だとスモークがかかる)
button_リセット(時刻、stateをリセット)

useReducer	stateを管理しましょう。タイマーの状態（mode, time, isRunning）管理
useRef	setInterval で再生/停止の制御
Redux	stateの管理はreduxのstoreで行う。計測履歴や設定（例：初期カウント時間）の保存。slice.tsを書いてsliceをreduxのstoreに登録

テスト（reducer）タイマーstateの状態遷移テスト（start, pause, resetなど）
テスト（UI）	  トースト表示・ボタンクリック動作の確認
テスト（非同期通信）モックAPI送信